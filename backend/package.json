{
  "name": "telemaster-pro",
  "version": "1.0.0",
  "description": "Sistema de transmisión profesional para televisión",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000 --host=localhost",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "live-server --port=3000",
    "server": "node backend/server.js",
    "build": "echo 'Build system not required for development'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "broadcast",
    "streaming",
    "iptv",
    "television",
    "production"
  ],
  "author": "Tu Nombre",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "concurrently": "^7.6.0"
  }
}
